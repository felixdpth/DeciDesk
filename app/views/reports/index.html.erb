<h1>Hello, please upload your FEC report here 👎</h1>
<div>
  <%= render 'form', report: @report %>
</div>

<div>
  <%= current_user.email %>
  <%= current_user.lines.count %>
  <% if @reports %>
    <p>You have <%= @reports.count %> FEC uploaded already !</p>
  <% else %>
    <p>You have not imported any FEC yet !</p>
  <% end %>
</div>

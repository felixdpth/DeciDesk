 <div class="mt-5">
   <%= simple_form_for(@report, as: :report, method: :post, url: reports_path) do |f| %>
    <%= f.input :csv_file, as: :file, hint: 'Upload your FEC in csv format here', required: true %>
    <%= f.submit %>
  <% end %>
</div>
